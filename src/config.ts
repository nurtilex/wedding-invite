/**
 * ⚙️  ВСЕ ДАННЫЕ ПРИГЛАСИТЕЛЬНОГО — ТОЛЬКО ЗДЕСЬ.
 * Меняйте значения ниже, остальной код трогать не нужно.
 */

export type Lang = 'kg' | 'ru'
/** Строка, у которой есть версия на кыргызском и на русском. */
export type Localized = Record<Lang, string>

export const config = {
  /** Язык по умолчанию. Гость может переключить, либо зафиксируйте через ?lang=ru */
  defaultLang: 'kg' as Lang,

  /** Имена молодожёнов (в порядке отображения). */
  groom: { kg: 'Нуртилек', ru: 'Нуртилек' } satisfies Localized,
  bride: { kg: 'Айдай', ru: 'Айдай' } satisfies Localized,

  /**
   * Фото на обложке. Лежит в public/, путь от корня сайта.
   * Пустая строка — обложка будет просто тёмно-синей, без фото.
   */
  coverPhoto: '/images/image_one.jpeg',

  /** Фото-фон страницы приглашения (вторая страница). Пустая строка — просто тёмно-синий. */
  invitePhoto: '/images/image_intro.jpeg',

  /** Инициалы для монограммы в футере. */
  monogram: 'Н & А',

  /**
   * Дата и время торжества — простыми числами, без часовых поясов.
   * Так дата выглядит одинаково у гостя из Бишкека и из Москвы.
   * month: 1 = январь … 12 = декабрь
   */
  date: { year: 2026, month: 10, day: 12, hour: 16, minute: 0 },

  /**
   * Сбор гостей — час перед началом торжества.
   * Поставьте null, если сбор отдельно не объявляете: останется одно время.
   */
  gathering: { hour: 15, minute: 0 } as { hour: number; minute: number } | null,

  /** Площадка. */
  venue: {
    name: {
      kg: 'Банкетный холл «Эго»',
      ru: 'Банкетный холл «Эго»',
    } satisfies Localized,
    address: {
      kg: 'Проспект Аалы Токомбаева, 43',
      ru: 'Проспект Аалы Токомбаева, 43',
    } satisfies Localized,
  },

  /**
   * Организаторы торжества — «той ээлери».
   * Пустая строка = блок не выводится, поэтому сейчас он есть только в кыргызской версии.
   */
  hosts: {
    kg: 'Дүйшөн жана Назгүл',
    ru: '',
  } satisfies Localized,

  /** Палитра дресс-кода. Пустой массив — кружки не показываются, останется только подпись. */
  dressColors: [
    { hex: '#22375c', title: 'Тёмно-синий' },
    { hex: '#131519', title: 'Чёрный' },
    { hex: '#4e3a2b', title: 'Шоколадный' },
    { hex: '#24473f', title: 'Изумрудный' },
    { hex: '#4e5560', title: 'Графитовый' },
  ],

  /** Координаты площадки — из них собираются ссылки на карты. */
  coords: { lat: 42.8270295, lng: 74.6091513 },

  /**
   * Необязательно: готовые ссылки из «Поделиться» в 2ГИС / Google Maps.
   * Если оставить пустыми — ссылки соберутся из coords выше.
   */
  mapLinks: {
    dgis: 'https://go.2gis.com/aauOv',
    google: 'https://maps.app.goo.gl/sKgg5giW7JzdTd8m9',
  },

  music: {
    /** Положите файл в public/music/ и укажите путь. Пусто = кнопки музыки не будет. */
    src: '/music/wedding.mp3',
    /** true — музыка стартует при первом касании экрана (браузеры блокируют автоплей без него). */
    startOnFirstTap: true,
    /**
     * Подпись о треке внизу страницы. Пустая строка в `text` — подписи не будет.
     * Обязательна, только если трек под лицензией с требованием атрибуции (например CC BY).
     */
    credit: {
      text: 'Музыка: Lana Del Rey — «Young and Beautiful»',
      url: 'https://www.youtube.com/watch?v=_MWEapW4PaE&list=RD_MWEapW4PaE&start_radio=1',
    },
  },
} as const
